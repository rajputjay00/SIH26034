# ==============================================================================
# NIRIKSHAN / SIH26034 — Frontend Environment Configuration Template
# ==============================================================================

# Title displayed in browser header
NEXT_PUBLIC_APP_NAME="NIRIKSHAN — Legal Metrology Compliance & Inspection System"

# Base URL of the deployed FastAPI backend service (e.g. on Render, Railway, or VPS)
# In production, replace with your actual HTTPS backend domain:
NEXT_PUBLIC_API_URL="https://your-backend-domain.example"

# Optional local development override (defaults to http://127.0.0.1:8000/api/v1 if omitted):
# NEXT_PUBLIC_API_BASE_URL="http://127.0.0.1:8000/api/v1"
